package util

/*
* @param err : error
* @return void
* Gérer les erreurs de connexion à GCP
**********Objectif***************
 * 1. Référencer les erreurs de connexion à GCP
 * 2. Afficher les erreurs de connexion à GCP
 * 3. Corriger les erreurs de connexion à GCP
 * 4. Automatiser les erreurs de connexion à GCP
*/
func GestionErrorConnectToGCP(err error) {
	if err != nil {
		panic(err)
	}
}

func GestionErrorGetAllUrlAndName(err error) {
	if err != nil {
		panic(err)
	}
}
